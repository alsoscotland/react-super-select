var escapeMarkdown = require('../markdown/js/keys/escape').body;

var escapeKey = {

  nameAttr: "escape",
  displayName: "Escape Key",

  renderString: escapeMarkdown

};

module.exports = escapeKey;
